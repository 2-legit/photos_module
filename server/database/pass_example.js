const DB_PASS = 'change this password';
// Change this password to the correct password and save a copy with the name `pass.js`
// Note that the password may be different across hardware configurations

module.exports = DB_PASS;